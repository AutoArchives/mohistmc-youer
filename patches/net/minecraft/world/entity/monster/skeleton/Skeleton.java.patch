--- a/net/minecraft/world/entity/monster/skeleton/Skeleton.java
+++ b/net/minecraft/world/entity/monster/skeleton/Skeleton.java
@@ -15,11 +_,11 @@
 
 public class Skeleton extends AbstractSkeleton {
     private static final int TOTAL_CONVERSION_TIME = 300;
-    private static final EntityDataAccessor<Boolean> DATA_STRAY_CONVERSION_ID = SynchedEntityData.defineId(Skeleton.class, EntityDataSerializers.BOOLEAN);
+    public static final EntityDataAccessor<Boolean> DATA_STRAY_CONVERSION_ID = SynchedEntityData.defineId(Skeleton.class, EntityDataSerializers.BOOLEAN);
     public static final String CONVERSION_TAG = "StrayConversionTime";
     private static final int NOT_CONVERTING = -1;
     private int inPowderSnowTime;
-    private int conversionTime;
+    public int conversionTime;
 
     public Skeleton(EntityType<? extends Skeleton> p_480588_, Level p_479309_) {
         super(p_480588_, p_479309_);
@@ -92,7 +_,9 @@
     }
 
     protected void doFreezeConversion() {
+        if (!net.neoforged.neoforge.event.EventHooks.canLivingConvert(this, EntityType.STRAY, (timer) -> this.conversionTime = timer)) return;
         this.convertTo(EntityType.STRAY, ConversionParams.single(this, true, true), p_479949_ -> {
+            net.neoforged.neoforge.event.EventHooks.onLivingConvert(this, p_479949_);
             if (!this.isSilent()) {
                 this.level().levelEvent(null, 1048, this.blockPosition(), 0);
             }
