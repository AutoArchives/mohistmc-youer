--- a/net/minecraft/world/entity/animal/bee/Bee.java
+++ b/net/minecraft/world/entity/animal/bee/Bee.java
@@ -135,7 +_,7 @@
     private float rollAmountO;
     private int timeSinceSting;
     int ticksWithoutNectarSinceExitingHive = 0;
-    private int stayOutOfHiveCountdown = 0;
+    public int stayOutOfHiveCountdown = 0;
     private int numCropsGrownSincePollination = 0;
     private static final int COOLDOWN_BEFORE_LOCATING_NEW_HIVE = 200;
     int remainingCooldownBeforeLocatingNewHive;
@@ -490,7 +_,7 @@
         if (this.hivePos == null) {
             return null;
         } else {
-            return this.isTooFarAway(this.hivePos) ? null : this.level().getBlockEntity(this.hivePos, BlockEntityType.BEEHIVE).orElse(null);
+            return this.isTooFarAway(this.hivePos) ? null : this.level().getBlockEntity(this.hivePos) instanceof BeehiveBlockEntity hive ? hive : null;
         }
     }
 
@@ -502,7 +_,7 @@
         return this.getFlag(8);
     }
 
-    void setHasNectar(boolean p_479143_) {
+    public void setHasNectar(boolean p_479143_) {
         if (p_479143_) {
             this.resetTicksWithoutNectarSinceExitingHive();
         }
@@ -514,7 +_,7 @@
         return this.getFlag(4);
     }
 
-    private void setHasStung(boolean p_481211_) {
+    public void setHasStung(boolean p_481211_) {
         this.setFlag(4, p_481211_);
     }
 
@@ -652,8 +_,18 @@
     }
 
     @Override
+    @Deprecated // FORGE: use jumpInFluid instead
     protected void jumpInLiquid(TagKey<Fluid> p_481380_) {
+        this.jumpInLiquidInternal();
+    }
+
+    private void jumpInLiquidInternal() {
         this.setDeltaMovement(this.getDeltaMovement().add(0.0, 0.01, 0.0));
+    }
+
+    @Override
+    public void jumpInFluid(net.neoforged.neoforge.fluids.FluidType type) {
+        this.jumpInLiquidInternal();
     }
 
     @Override
