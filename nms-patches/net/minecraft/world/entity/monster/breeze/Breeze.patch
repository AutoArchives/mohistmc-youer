--- a/net/minecraft/world/entity/monster/breeze/Breeze.java
+++ b/net/minecraft/world/entity/monster/breeze/Breeze.java
@@ -78,7 +78,7 @@
 
     @Override
     public Brain<Breeze> getBrain() {
-        return super.getBrain();
+        return (Brain<Breeze>) super.getBrain(); // CraftBukkit - decompile error
     }
 
     @Override
@@ -246,6 +246,7 @@
 
     @Override
     public boolean canAttackType(EntityType<?> targetType) {
+        if (this.getTarget() != null) return this.getTarget().getType() == targetType; // SPIGOT-7957: Allow attack if target from brain was set
         return targetType == EntityType.PLAYER || targetType == EntityType.IRON_GOLEM;
     }
 
